import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        height: '100%',
        width: '100%',
        flexDirection: 'row',
        columnGap: 15,
        rowGap: 22,
        marginHorizontal: 20,
        flexWrap: 'wrap',
        alignItems: 'stretch',
        justifyContent: 'center',
    }
});
